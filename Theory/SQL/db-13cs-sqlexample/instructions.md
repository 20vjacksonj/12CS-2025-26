# Admin Scripts
## Purpose
* main.py = a menu system to list students, list courses and assign student to a course using each of the primary keys from student and course
## Folder Structure
### admin folder
* A set of python scripts to:-
* define tables with records and fields with specific data types
* insert data into the course and student tables

## General Principles
* to run an **admin** script open a shell tab and type for example:-
	```
	python checkschema.py

* myDatabase.db = an encrypted file that is the database management system
* there are a set of text files used to populate the tables with data

**Tables**
student
course
studentoncourse